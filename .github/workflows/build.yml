name: build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        include:
          - platform: x64
            rid: win-x64
          - platform: ARM64
            rid: win-arm64
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: '9.0.x'

      - name: Build
        shell: pwsh
        run: |
          dotnet build src\ZedWorkspaces\ZedWorkspaces.csproj `
            -c Release `
            -p:Platform=${{ matrix.platform }} `
            -r ${{ matrix.rid }}

      - name: Stage plugin folder
        shell: pwsh
        run: |
          $outDir = "src/ZedWorkspaces/bin/${{ matrix.platform }}/Release/net9.0-windows10.0.26100.0/${{ matrix.rid }}"
          if (-not (Test-Path "$outDir/plugin.json")) {
            throw "plugin.json not found in $outDir"
          }
          $stage = "artifact/ZedWorkspaces"
          New-Item -ItemType Directory -Force $stage | Out-Null
          Copy-Item "$outDir/*" $stage -Recurse -Force

      - name: Package
        shell: pwsh
        run: |
          Compress-Archive -Path "artifact/ZedWorkspaces" -DestinationPath "ZedWorkspaces-${{ matrix.rid }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ZedWorkspaces-${{ matrix.rid }}
          path: ZedWorkspaces-${{ matrix.rid }}.zip

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: ZedWorkspaces-${{ matrix.rid }}.zip
          fail_on_unmatched_files: false

      - name: Changelog
        if: startsWith(github.ref, 'refs/tags/') && matrix.rid == 'win-x64'
        run: npx changelogithub@15.0.5
        env:
          GITHUB_TOKEN: ${{ github.token }}